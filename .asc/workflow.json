{
  "env": {
    "APP_ID": "6770222036",
    "PROJECT_PATH": "Yomi.xcodeproj",
    "SCHEME": "Yomi",
    "CONFIGURATION": "Release",
    "EXPORT_OPTIONS": ".asc/export-options.plist",
    "VERSION": "",
    "GROUP": "",
    "SUBMIT_BETA": "true"
  },
  "workflows": {
    "testflight_internal": {
      "description": "Archive yomy, export an IPA, and upload to TestFlight for internal testing. Build number is auto-managed by the Xcode Run Script phase (git commit count). Signing uses the App Store Connect API key from .env (ASC_KEY_ID / ASC_ISSUER_ID / ASC_PRIVATE_KEY_PATH) when set, so it does not depend on an Apple ID being signed into Xcode.",
      "steps": [
        {
          "name": "validate_version",
          "run": "if [ -z \"$VERSION\" ]; then echo \"VERSION is required (e.g. asc workflow run testflight_internal VERSION:1.0)\" >&2; exit 1; fi"
        },
        {
          "name": "archive",
          "run": "AUTH_FLAGS=\"\"; if [ -n \"$ASC_PRIVATE_KEY_PATH\" ]; then AUTH_FLAGS=\"--xcodebuild-flag=-authenticationKeyPath --xcodebuild-flag=$ASC_PRIVATE_KEY_PATH --xcodebuild-flag=-authenticationKeyID --xcodebuild-flag=$ASC_KEY_ID --xcodebuild-flag=-authenticationKeyIssuerID --xcodebuild-flag=$ASC_ISSUER_ID\"; fi; asc xcode archive --project \"$PROJECT_PATH\" --scheme \"$SCHEME\" --configuration \"$CONFIGURATION\" --archive-path \".asc/artifacts/yomy-$VERSION.xcarchive\" --clean --overwrite --xcodebuild-flag=-destination --xcodebuild-flag=generic/platform=iOS --xcodebuild-flag=-allowProvisioningUpdates $AUTH_FLAGS --xcodebuild-flag=MARKETING_VERSION=$VERSION --output json",
          "outputs": {
            "ARCHIVE_PATH": "$.archive_path"
          }
        },
        {
          "name": "export",
          "run": "AUTH_FLAGS=\"\"; if [ -n \"$ASC_PRIVATE_KEY_PATH\" ]; then AUTH_FLAGS=\"--xcodebuild-flag=-authenticationKeyPath --xcodebuild-flag=$ASC_PRIVATE_KEY_PATH --xcodebuild-flag=-authenticationKeyID --xcodebuild-flag=$ASC_KEY_ID --xcodebuild-flag=-authenticationKeyIssuerID --xcodebuild-flag=$ASC_ISSUER_ID\"; fi; asc xcode export --archive-path ${steps.archive.ARCHIVE_PATH} --export-options \"$EXPORT_OPTIONS\" --ipa-path \".asc/artifacts/yomy-$VERSION.ipa\" --overwrite --xcodebuild-flag=-allowProvisioningUpdates $AUTH_FLAGS --output json",
          "outputs": {
            "IPA_PATH": "$.ipa_path"
          }
        },
        {
          "name": "upload",
          "run": "asc builds upload --app \"$APP_ID\" --ipa ${steps.export.IPA_PATH} --wait --output json"
        }
      ]
    },
    "testflight_external": {
      "description": "Archive yomy, export an IPA, upload to TestFlight, attach to an External Beta Group, and submit for Beta App Review. Pass GROUP:<name|id>. Pass SUBMIT_BETA:false to skip review submission for subsequent builds of an already-approved version. Signing uses the App Store Connect API key from .env (ASC_KEY_ID / ASC_ISSUER_ID / ASC_PRIVATE_KEY_PATH) when set, so it does not depend on an Apple ID being signed into Xcode.",
      "steps": [
        {
          "name": "validate_inputs",
          "run": "if [ -z \"$VERSION\" ]; then echo \"VERSION is required (e.g. VERSION:1.0)\" >&2; exit 1; fi; if [ -z \"$GROUP\" ]; then echo \"GROUP is required (External Beta Group name or ID; e.g. GROUP:'Public Beta')\" >&2; exit 1; fi"
        },
        {
          "name": "archive",
          "run": "AUTH_FLAGS=\"\"; if [ -n \"$ASC_PRIVATE_KEY_PATH\" ]; then AUTH_FLAGS=\"--xcodebuild-flag=-authenticationKeyPath --xcodebuild-flag=$ASC_PRIVATE_KEY_PATH --xcodebuild-flag=-authenticationKeyID --xcodebuild-flag=$ASC_KEY_ID --xcodebuild-flag=-authenticationKeyIssuerID --xcodebuild-flag=$ASC_ISSUER_ID\"; fi; asc xcode archive --project \"$PROJECT_PATH\" --scheme \"$SCHEME\" --configuration \"$CONFIGURATION\" --archive-path \".asc/artifacts/yomy-$VERSION.xcarchive\" --clean --overwrite --xcodebuild-flag=-destination --xcodebuild-flag=generic/platform=iOS --xcodebuild-flag=-allowProvisioningUpdates $AUTH_FLAGS --xcodebuild-flag=MARKETING_VERSION=$VERSION --output json",
          "outputs": {
            "ARCHIVE_PATH": "$.archive_path"
          }
        },
        {
          "name": "export",
          "run": "AUTH_FLAGS=\"\"; if [ -n \"$ASC_PRIVATE_KEY_PATH\" ]; then AUTH_FLAGS=\"--xcodebuild-flag=-authenticationKeyPath --xcodebuild-flag=$ASC_PRIVATE_KEY_PATH --xcodebuild-flag=-authenticationKeyID --xcodebuild-flag=$ASC_KEY_ID --xcodebuild-flag=-authenticationKeyIssuerID --xcodebuild-flag=$ASC_ISSUER_ID\"; fi; asc xcode export --archive-path ${steps.archive.ARCHIVE_PATH} --export-options \"$EXPORT_OPTIONS\" --ipa-path \".asc/artifacts/yomy-$VERSION.ipa\" --overwrite --xcodebuild-flag=-allowProvisioningUpdates $AUTH_FLAGS --output json",
          "outputs": {
            "IPA_PATH": "$.ipa_path"
          }
        },
        {
          "name": "publish",
          "run": "SUBMIT_FLAGS=\"\"; if [ \"$SUBMIT_BETA\" = \"true\" ]; then SUBMIT_FLAGS=\"--submit --confirm\"; fi; asc publish testflight --app \"$APP_ID\" --ipa ${steps.export.IPA_PATH} --group \"$GROUP\" --wait $SUBMIT_FLAGS --output json"
        }
      ]
    }
  }
}
